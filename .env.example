# Forge Digital — environment configuration
#
# The site is a static marketing/lead-gen site. The only configurable value is
# the Formspree endpoint that the enquiry form posts to. It's already hardcoded
# in lib/contact.ts (delivering to forge100000@gmail.com), so this is optional —
# set it only to override the endpoint per environment.
#
# NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/mrevvglw
